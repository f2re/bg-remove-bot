# Background Removal Telegram Bot

Telegram бот для удаления фона с изображений с использованием AI и интеграцией платежей через Robokassa.

## Возможности

### Для пользователей
- 🎁 3 бесплатные обработки изображений при регистрации
- 📸 Автоматическое удаление фона с изображений
- 💎 Покупка пакетов изображений через Robokassa
- 📊 Просмотр баланса и истории
- 💬 Обратная связь с администратором
- ℹ️ Доступ к правовой информации (оферта, возврат, конфиденциальность)

### Для администраторов
- 📊 Статистика бота (пользователи, обработанные изображения, выручка)
- 💬 Управление обращениями в поддержку
- ➕ Ручное добавление генераций пользователю
- 💵 Оформление возвратов
- 📦 Просмотр заказов

## Технологический стек

- **Python 3.11+**
- **aiogram 3.4.1** - Telegram Bot Framework
- **SQLAlchemy 2.0.25** - ORM для работы с БД
- **PostgreSQL** - Основная база данных
- **Alembic** - Миграции базы данных
- **OpenRouter API** - AI для обработки изображений
- **Robokassa** - Платежная система
- **Docker** - Контейнеризация

## Структура проекта

```
/
├── app/
│   ├── __init__.py
│   ├── bot.py                 # Главный файл бота
│   ├── config.py              # Конфигурация
│   ├── handlers/              # Обработчики команд
│   │   ├── user.py            # Пользовательские команды
│   │   ├── admin.py           # Админ-панель
│   │   ├── payment.py         # Обработка платежей
│   │   └── support.py         # Поддержка
│   ├── services/              # Сервисы
│   │   ├── openrouter.py      # API OpenRouter
│   │   ├── image_processor.py # Обработка изображений
│   │   ├── robokassa.py       # Платежи Robokassa
│   │   └── prompt_builder.py  # Генерация промптов
│   ├── database/              # База данных
│   │   ├── models.py          # SQLAlchemy модели
│   │   ├── crud.py            # CRUD операции
│   │   └── __init__.py        # Инициализация БД
│   ├── keyboards/             # Клавиатуры
│   │   ├── user_kb.py         # Пользовательские
│   │   └── admin_kb.py        # Админские
│   └── utils/                 # Утилиты
│       ├── validators.py      # Валидация данных
│       └── decorators.py      # Декораторы
├── alembic/                   # Миграции БД
│   ├── versions/              # Файлы миграций
│   ├── env.py
│   └── script.py.mako
├── static/
│   └── legal/                 # PDF документы
├── .env.example
├── .gitignore
├── requirements.txt
├── docker-compose.yml
├── Dockerfile
└── README.md
```

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd bg-remove-bot
```

### 2. Создание .env файла

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Обязательные параметры:
- `BOT_TOKEN` - токен вашего бота от @BotFather
- `ADMIN_IDS` - ID администраторов через запятую
- `DB_PASSWORD` - пароль для PostgreSQL
- `OPENROUTER_API_KEY` - API ключ OpenRouter
- `ROBOKASSA_LOGIN` - логин в Robokassa
- `ROBOKASSA_PASSWORD1` - пароль 1 Robokassa
- `ROBOKASSA_PASSWORD2` - пароль 2 Robokassa

### 3. Запуск через Docker

```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL на порту 5432
- Redis на порту 6379
- Telegram Bot

### 4. Запуск локально (без Docker)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск PostgreSQL (если еще не запущен)
# Например, через Docker:
docker run -d \
  --name postgres \
  -e POSTGRES_DB=bg_removal_bot \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=your_password \
  -p 5432:5432 \
  postgres:15

# Запуск миграций
alembic upgrade head

# Запуск бота
python -m app.bot
```

## База данных

### Структура таблиц

**users** - Пользователи бота
- `id` - Уникальный ID
- `telegram_id` - Telegram ID пользователя
- `username` - Username в Telegram
- `first_name` - Имя пользователя
- `free_images_left` - Остаток бесплатных изображений
- `total_images_processed` - Всего обработано изображений

**packages** - Пакеты изображений
- `id` - Уникальный ID
- `name` - Название пакета
- `images_count` - Количество изображений
- `price_rub` - Цена в рублях
- `is_active` - Активен ли пакет

**orders** - Заказы
- `id` - Уникальный ID
- `user_id` - ID пользователя
- `package_id` - ID пакета
- `robokassa_invoice_id` - ID счета в Robokassa
- `amount` - Сумма
- `status` - Статус (pending, paid, refunded)

**processed_images** - Обработанные изображения
- `id` - Уникальный ID
- `user_id` - ID пользователя
- `original_file_id` - ID оригинального файла
- `processed_file_id` - ID обработанного файла
- `prompt_used` - Использованный промпт
- `is_free` - Бесплатная ли обработка

**support_tickets** - Обращения в поддержку
- `id` - Уникальный ID
- `user_id` - ID пользователя
- `message` - Сообщение
- `status` - Статус (open, in_progress, resolved)
- `admin_response` - Ответ администратора

**admins** - Администраторы
- `id` - Уникальный ID
- `telegram_id` - Telegram ID администратора
- `role` - Роль (admin, super_admin)

### Миграции

Создание новой миграции:
```bash
alembic revision --autogenerate -m "Description"
```

Применение миграций:
```bash
alembic upgrade head
```

Откат миграции:
```bash
alembic downgrade -1
```

## Конфигурация

### Переменные окружения

Все параметры настраиваются через файл `.env`:

```env
# Telegram
BOT_TOKEN=your_bot_token
ADMIN_IDS=123456789,987654321

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bg_removal_bot
DB_USER=postgres
DB_PASSWORD=your_password

# OpenRouter
OPENROUTER_API_KEY=your_api_key
OPENROUTER_MODEL=nano-banana-ai/model-name

# Robokassa
ROBOKASSA_LOGIN=your_login
ROBOKASSA_PASSWORD1=your_password1
ROBOKASSA_PASSWORD2=your_password2
ROBOKASSA_TEST_MODE=False

# Pricing (в копейках)
PACKAGE_1_PRICE=5000
PACKAGE_5_PRICE=20000
PACKAGE_10_PRICE=35000
PACKAGE_50_PRICE=150000

# Free images
FREE_IMAGES_COUNT=3
```

## Использование

### Команды бота

**Пользовательские команды:**
- `/start` - Запуск бота и регистрация
- `📸 Обработать изображение` - Начать обработку
- `💎 Купить пакет` - Просмотр доступных пакетов
- `📊 Мой баланс` - Просмотр баланса
- `ℹ️ Информация` - Информация о боте
- `💬 Поддержка` - Обратиться в поддержку

**Административные команды:**
- `/admin` - Открыть админ-панель
- `/ticket <ID>` - Просмотр обращения

### Обработка изображений

1. Пользователь отправляет фото боту
2. Бот анализирует изображение:
   - Определяет сложные края (волосы, мех)
   - Проверяет наличие прозрачных объектов
   - Детектирует размытие
3. Строит оптимальный промпт
4. Отправляет запрос в OpenRouter API
5. Возвращает обработанное изображение

### Платежи

1. Пользователь выбирает пакет
2. Генерируется уникальный invoice_id
3. Создается ссылка для оплаты в Robokassa
4. После оплаты webhook обновляет статус заказа
5. Изображения начисляются на баланс

## API Интеграции

### OpenRouter

Для работы требуется API ключ OpenRouter. Получить можно на [openrouter.ai](https://openrouter.ai).

Модель по умолчанию: `nano-banana-ai/model-name`

### Robokassa

Для приема платежей требуется:
1. Зарегистрироваться на [robokassa.ru](https://robokassa.ru)
2. Создать магазин
3. Получить логин и пароли (Password#1 и Password#2)
4. Настроить ResultURL для webhook'ов

## Разработка

### Добавление новых обработчиков

Создайте файл в `app/handlers/`:

```python
from aiogram import Router

router = Router()

@router.message(...)
async def handler(message: Message):
    # Ваш код
    pass
```

Зарегистрируйте роутер в `app/bot.py`:

```python
from app.handlers import your_handler

dp.include_router(your_handler.router)
```

### Добавление новых моделей

1. Создайте модель в `app/database/models.py`
2. Создайте CRUD операции в `app/database/crud.py`
3. Создайте миграцию:
   ```bash
   alembic revision --autogenerate -m "Add new model"
   ```
4. Примените миграцию:
   ```bash
   alembic upgrade head
   ```

## Мониторинг и логи

Логи бота выводятся в stdout. Для просмотра логов в Docker:

```bash
docker-compose logs -f bot
```

Уровни логирования настраиваются в `app/bot.py`:

```python
logging.basicConfig(level=logging.INFO)
```

## Безопасность

- Никогда не коммитьте файл `.env` с реальными данными
- Используйте сильные пароли для базы данных
- Ограничьте доступ к админским функциям через `ADMIN_IDS`
- Регулярно обновляйте зависимости
- Используйте HTTPS для webhook'ов Robokassa

## Troubleshooting

### Бот не отвечает

1. Проверьте, запущен ли бот: `docker-compose ps`
2. Проверьте логи: `docker-compose logs bot`
3. Убедитесь, что токен бота правильный

### Ошибки базы данных

1. Проверьте, запущена ли PostgreSQL: `docker-compose ps db`
2. Проверьте подключение:
   ```bash
   docker-compose exec db psql -U postgres -d bg_removal_bot
   ```
3. Примените миграции: `alembic upgrade head`

### Проблемы с платежами

1. Проверьте настройки Robokassa (логин, пароли)
2. Убедитесь, что `ROBOKASSA_TEST_MODE=True` для тестирования
3. Проверьте настройки ResultURL в личном кабинете Robokassa

## Лицензия

MIT License

## Поддержка

По вопросам обращайтесь к администратору бота или создавайте issue в репозитории.
